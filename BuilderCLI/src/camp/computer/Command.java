package camp.computer;

public class Command {

    String keyword = null;

    /*
    public interface Task {
        void execute(Context object);
    }

    Task task;

    public Command(String keyword, Task task) {
    }
    */

}
